services:
  - type: web
    name: retail-agent-backend
    runtime: python
    buildCommand: pip install -r requirements.txt && pip install uvicorn
    startCommand: python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false